name: Sync Repo A to Repo B

on:
  push:
    branches:
      - '**'
  create:
    branches:
      - '**'
  pull_request:
    types:
      - closed

jobs:
  sync:
    if: github.event.pull_request.merged == true || github.event_name != 'pull_request'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo A
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Git
        run: |
          git config --global user.email "sync-bot@example.com"
          git config --global user.name "Repo Sync Bot"
          git config --global push.default simple

      - name: Push to Repo B
        env:
          TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          DEST_REPO: ${{ secrets.DESTINATION_REPO }}
        run: |
          echo "Setting up remote..."
          
          REMOTE_URL="https://x-access-token:${TOKEN}@github.com/${DEST_REPO}.git"

          echo "Remote URL set (hidden by GitHub logs for security)"

          git remote add repo_b "$REMOTE_URL"

          echo "Pushing all branches..."
          git push repo_b --all --force

          echo "Pushing all tags..."
          git push repo_b --tags --force
