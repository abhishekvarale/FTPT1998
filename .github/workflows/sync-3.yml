name: Mirror Repo A to Repo B

on:
  push:
    branches: ["**"]
    tags: ["**"]
  create:
    branches: ["**"]
    tags: ["**"]
  delete:
    branches: ["**"]
    tags: ["**"]
  pull_request:
    types: [opened, reopened, synchronize, closed]
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  mirror:
    if: github.event_name != 'pull_request' || github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: Mirror Repo A to Repo B (clean push)
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          DESTINATION_REPO: ${{ secrets.DESTINATION_REPO }}
        run: |
          echo "🔁 Cloning Repo A as mirror..."
          git clone --mirror https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }} repo-mirror
          cd repo-mirror

          echo "🧹 Cleaning protected refs like refs/pull/*..."
          git for-each-ref --format='%(refname)' refs/pull/ | xargs -r -n 1 git update-ref -d || true
          rm -rf refs/pull/

          echo "🔗 Setting new remote (Repo B)..."
          git remote set-url origin https://x-access-token:${GH_TOKEN}@github.com/${DESTINATION_REPO}.git

          echo "🚀 Force pushing each ref (branches & tags)..."
          for ref in $(git for-each-ref --format='%(refname)'); do
            git push --force origin "$ref":"$ref" || echo "⚠️ Skipped $ref due to push conflict"
          done

          echo "✅ Repo A successfully mirrored to Repo B without PR refs."
